language: node_js
node_js:
- "9"
sudo: true
dist: trusty

branches:
  only:
  - master

cache:
    directories:
    - node_modules

before_script:
- export CHROME_BIN=/usr/bin/google-chrome
- export DISPLAY=:99.0
- sh -e /etc/init.d/xvfb start
- sudo apt-get update
- sudo apt-get install -y libappindicator1 fonts-liberation
- wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
- sudo dpkg -i google-chrome*.deb

install:
- npm install -g yarn

script:
- npm run e2e
- ng build
- yarn lint
- yarn build --base-href   https://alexthegreat1509.github.io/KillerAppFun4-FrontEnd/
  
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist/Killerapp-FrontEnd
  on:
    branch: master
